{
  "name": "pic2vid",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "build": "npm install && npx playwright install --with-deps chromium"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "playwright": "^1.60.0",
    "uuid": "^9.0.0"
  }
}
